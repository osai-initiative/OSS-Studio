export function appPage(): string {
  return enhancePage(basePage().replace('<title>Studio</title>', '<title>OSS Studio</title>'));
}

export function oauthCallbackPage(): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Signing in with ChatGPT · OSS Studio</title><style>body{margin:0;min-height:100vh;display:grid;place-items:center;background:#000;color:#f5f5f1;font:15px system-ui,sans-serif}.card{max-width:420px;padding:28px;border:1px solid #242424;border-radius:16px;background:#0b0b0c}.muted{color:#a7a7a0;line-height:1.6}</style></head><body><main class="card"><h1>Finishing ChatGPT sign-in…</h1><p class="muted" id="status">Securely saving your browser session. This page will return to OSS Studio automatically.</p></main><script type="module">import{completeLogin}from"https://esm.sh/@openai-oauth/web@2.0.0";const s=document.querySelector('#status');try{await completeLogin();location.replace('/studio')}catch(e){s.textContent=e?.message||'ChatGPT sign-in could not be completed. Return to Studio and try again.'}</script></body></html>`;
}

function basePage(): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#000"><title>OSS Studio</title><style>
@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700;800&family=Playfair+Display:ital,wght@0,600;0,700;1,600&display=swap');
:root{--bg:#000;--panel:#0b0b0c;--panel2:#131316;--line:rgba(255,255,255,.07);--line2:rgba(255,255,255,.14);--text:#f5f5f1;--muted:#a7a7a0;--faint:rgba(255,255,255,.38);--green:#34d17e;--green-dim:rgba(52,209,126,.12)}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:15px Manrope,system-ui,sans-serif;-webkit-font-smoothing:antialiased}.shell{min-height:100dvh;display:grid;grid-template-columns:260px 1fr}.side{background:var(--panel);border-right:1px solid var(--line);padding:24px 16px;display:flex;flex-direction:column;min-height:0}.logo{font:700 22px 'Playfair Display',serif;letter-spacing:-.06em;padding:4px 10px 26px}.logo i{color:var(--green)}.new{display:flex;align-items:center;gap:8px;background:transparent;border:1px solid var(--line2);color:var(--text);border-radius:12px;padding:12px 13px;font:600 13px Manrope;cursor:pointer;transition:border-color .16s,background .16s}.new:hover{border-color:rgba(52,209,126,.55);background:var(--green-dim)}.history{margin-top:18px;display:flex;flex-direction:column;flex:1;min-height:0}.hist-label{font:500 11px 'DM Mono';letter-spacing:.09em;text-transform:uppercase;color:var(--faint);padding:0 10px 8px}#histList{overflow-y:auto;display:grid;gap:2px;min-height:0}.hist-item{display:flex;align-items:center;gap:6px;background:transparent;border:0;color:var(--faint);text-align:left;padding:9px 10px;border-radius:9px;font:500 12px Manrope;cursor:pointer;width:100%;transition:color .15s,background .15s}.hist-item:hover{color:var(--text);background:rgba(255,255,255,.05)}.hist-item.active{color:var(--text);background:rgba(255,255,255,.08)}.hist-title{flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.hist-del{flex:none;color:var(--faint);padding:0 5px;border-radius:6px;font-size:14px;line-height:1;opacity:0;transition:opacity .15s}.hist-item:hover .hist-del{opacity:1}.hist-del:hover{color:#ff6b6b}.hist-empty{padding:6px 10px;color:var(--faint);font-size:12px}.side small{margin-top:auto;color:var(--faint);line-height:1.6;padding:12px;font-size:12px}.main{max-width:1000px;width:100%;margin:0 auto;display:flex;flex-direction:column;min-height:100dvh;padding:26px 40px}.top{display:flex;justify-content:space-between;align-items:center}.status{font:12px 'DM Mono';color:var(--muted);display:flex;align-items:center;gap:8px}.status b{color:var(--green);animation:blink 2.4s ease-in-out infinite}.model{border:1px solid var(--line2);background:var(--panel);border-radius:99px;padding:7px 13px;font:12px 'DM Mono';color:var(--muted)}.welcome{margin:auto 0 44px;max-width:720px}.eyebrow{display:inline-flex;align-items:center;gap:9px;color:var(--green);font:500 12px 'DM Mono';letter-spacing:.1em;text-transform:uppercase}.eyebrow::before{content:'';width:24px;height:1px;background:var(--green)}.welcome h1{font:600 clamp(40px,6.5vw,72px)/1 'Playfair Display',serif;letter-spacing:-.052em;margin:18px 0 14px}.welcome h1 em{color:var(--green)}.welcome p{color:var(--muted);line-height:1.7;max-width:540px;margin:0}.chips{display:grid;gap:10px;margin-top:32px;max-width:560px}.chip{display:flex;align-items:center;justify-content:space-between;gap:14px;text-align:left;background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:14px 16px;color:var(--text);font:500 13px Manrope;line-height:1.5;cursor:pointer;transition:border-color .16s,background .16s,transform .16s}.chip .arrow{color:var(--green);opacity:0;transform:translateX(-5px);transition:opacity .2s,transform .2s;font-size:14px}.chip:hover{border-color:var(--line2);background:var(--panel2);transform:translateX(4px)}.chip:hover .arrow{opacity:1;transform:none}.feed{display:grid;gap:26px;max-width:800px;margin:30px 0 12px}.message{line-height:1.7;white-space:pre-wrap;font-size:15px;overflow-wrap:anywhere}.message.user{background:var(--green-dim);border:1px solid rgba(52,209,126,.24);border-radius:18px 18px 4px 18px;padding:14px 18px;justify-self:end;max-width:78%;color:#dff7ea}.message.assistant{padding:2px 4px;color:#eaeae4;white-space:normal}.message.assistant :last-child{margin-bottom:0}.message.assistant p{margin:0 0 12px}.message.assistant :is(h1,h2,h3,h4){font-weight:700;line-height:1.25;margin:20px 0 10px}.message.assistant h1{font-size:20px}.message.assistant h2{font-size:18px}.message.assistant h3{font-size:16px}.message.assistant :is(ul,ol){margin:0 0 12px;padding-left:22px}.message.assistant li{margin:3px 0}.message.assistant code{background:rgba(255,255,255,.09);border-radius:6px;padding:2px 6px;font:12px 'DM Mono';color:#9ff0c5}.message.assistant pre{background:#0d0d0f;border:1px solid var(--line);border-radius:12px;padding:14px;overflow-x:auto;margin:0 0 14px;position:relative}.message.assistant pre code{background:transparent;padding:0;font-size:12.5px;line-height:1.6;color:#d8f7e6;display:block}.message.assistant blockquote{border-left:2px solid var(--green);margin:0 0 14px;padding:2px 16px;color:var(--muted)}.message.assistant a{color:var(--green);text-decoration:none;border-bottom:1px solid rgba(52,209,126,.35)}.message.assistant a:hover{border-bottom-color:var(--green)}.message.assistant hr{border:0;border-top:1px solid var(--line);margin:16px 0}.copy-btn{position:absolute;top:8px;right:8px;background:var(--panel2);border:1px solid var(--line2);color:var(--muted);border-radius:7px;font:600 11px Manrope;padding:4px 9px;cursor:pointer;opacity:0;transition:opacity .15s}.message.assistant pre:hover .copy-btn{opacity:1}.copy-btn:hover{color:var(--text)}.dots{display:inline-flex;gap:5px;padding:6px 2px}.dots i{width:6px;height:6px;border-radius:50%;background:var(--green);animation:dot 1.2s infinite}.dots i:nth-child(2){animation-delay:.15s}.dots i:nth-child(3){animation-delay:.3s}.message.streaming::after{content:'';display:inline-block;width:8px;height:14px;background:var(--green);margin-left:3px;vertical-align:-2px;animation:blink 1s steps(1) infinite}.composer{position:sticky;bottom:0;background:linear-gradient(transparent,rgba(0,0,0,.88) 26%);backdrop-filter:blur(8px);padding:32px 0 16px}.box{background:var(--panel);border:1px solid var(--line2);border-radius:18px;padding:12px 12px 10px;box-shadow:0 22px 60px -22px rgba(0,0,0,.95);transition:border-color .2s,box-shadow .2s}.box:focus-within{border-color:rgba(52,209,126,.5);box-shadow:0 22px 60px -22px rgba(0,0,0,.95),0 0 0 3px rgba(52,209,126,.09)}.box textarea{display:block;border:0;outline:0;resize:none;width:100%;min-height:64px;padding:8px 6px 14px;background:transparent;font:15px Manrope;color:var(--text);line-height:1.6}.box textarea::placeholder{color:var(--faint)}.tools{display:flex;align-items:center;gap:8px;padding:2px;flex-wrap:wrap}.seg{display:flex;align-items:center;background:rgba(255,255,255,.045);border:1px solid var(--line);border-radius:11px;padding:3px;gap:2px}.seg .mode{background:transparent;border:0;color:var(--faint);padding:7px 12px;border-radius:8px}.seg .mode:hover{color:var(--text)}.seg .mode.active{background:var(--panel2);color:var(--text);box-shadow:inset 0 0 0 1px var(--line2)}.right{margin-left:auto;display:flex;align-items:center;gap:8px}.mode{background:var(--panel2);border:1px solid var(--line);color:var(--muted);padding:8px 12px;border-radius:10px;font:600 12px Manrope;cursor:pointer;transition:color .16s,border-color .16s,background .16s}.mode:hover{color:var(--text);border-color:var(--line2)}.send{border:0;border-radius:10px;background:var(--green);color:#03140b;padding:9px 16px;font:700 13px Manrope;cursor:pointer;transition:filter .16s,transform .1s}.send:hover{filter:brightness(1.12)}.send:active{transform:scale(.96)}.send:disabled{opacity:.5;cursor:default;filter:none}.hint{font:11px 'DM Mono';color:var(--faint);padding:10px 4px 0}.swarm{font:12px 'DM Mono';color:var(--green);margin:0 0 8px;padding:0 4px}.hidden{display:none}@keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}@keyframes dot{0%,60%,100%{opacity:.2;transform:translateY(0)}30%{opacity:1;transform:translateY(-3px)}}@media(max-width:760px){.shell{display:block}.side{display:none}.main{padding:18px 16px}.welcome h1{font-size:44px}.message.user{max-width:92%}.chips{max-width:100%}.composer{padding-bottom:56px}.right{flex-wrap:nowrap;gap:6px;margin-left:0;width:100%}.right .mode{flex:1 1 auto;min-width:0}.right select{max-width:45%}.right #send{flex:0 1 auto;padding:9px 14px}}
</style></head><body><div class="shell"><aside class="side"><div class="logo">Studio</div><button class="new" onclick="newChat()">＋ New conversation</button><div class="history"><div class="hist-label">Recents</div><div id="histList"></div></div><small>Studio, clear thinking.<br>Fast when it matters. Smart when it counts.</small></aside><main class="main"><header class="top"><span class="status"><b>●</b> Studio models ready</span><span class="model" id="modelLabel">FAST · Laguna XS</span></header><section class="welcome" id="welcome"><span class="eyebrow">Open-source AI, simply useful</span><h1>What would you like to <em>figure out?</em></h1><p>Ask a question, hand over a task, or put a swarm on a difficult problem. You choose the pace.</p><div class="chips"><button class="chip" data-prompt="Explain the observer pattern with a real-world example, then give a minimal code sketch."><span>Explain the observer pattern with a real-world example</span><span class="arrow">→</span></button><button class="chip" data-prompt="Brainstorm three marketing angles for an open-source developer tool."><span>Brainstorm three marketing angles for an open-source tool</span><span class="arrow">→</span></button><button class="chip" data-prompt="Help me plan a small side project: a self-hosted bookmark manager. What stack and first steps?"><span>Plan a self-hosted bookmark manager side project</span><span class="arrow">→</span></button></div></section><section id="feed" class="feed"></section><div class="composer"><div id="swarmNote" class="swarm hidden">Swarm will launch 300 fast researchers, then one smart delegator.</div><div class="box"><textarea id="prompt" placeholder="Ask anything…" autofocus></textarea><div class="tools"><div class="seg"><button class="mode active" data-mode="chat">Chat</button><button class="mode" data-mode="agent">Agent</button><button class="mode" data-mode="swarm">Agent Swarm</button></div><div class="right"><select class="mode" id="speed" aria-label="Model family"><option value="fast">Fast</option><option value="smart">Smart</option><option value="advanced">Advanced</option><option value="chatgpt" id="chatgptFamily" hidden>ChatGPT / Codex</option></select><select class="mode hidden" id="advancedModel" aria-label="Advanced model"><option value="logfare/kimi-k3">Kimi K3</option><option value="logfare/deepseek-v4-flash">DeepSeek V4 Flash</option></select><select class="mode" id="reasoning" aria-label="Reasoning"><option value="none">Reasoning: None</option><option value="low" selected>Reasoning: Light</option><option value="medium">Reasoning: Medium</option><option value="xhigh">Reasoning: Max</option></select><button class="send" id="send">Send ↗</button></div></div></div><div class="hint">Enter to send · Shift + Enter for a new line</div></div></main></div><script src="https://cdn.jsdelivr.net/npm/marked@12.0.2/marked.min.js"></script><script>${appScript()}</script></body></html>`;
}

function enhancePage(page: string): string {
  const extra = `<style>
.studio-utility{position:fixed;left:16px;bottom:18px;z-index:45;display:grid;gap:6px;width:228px}.studio-utility button{display:flex;align-items:center;gap:8px;width:100%;background:rgba(11,11,12,.92);border:1px solid var(--line);border-radius:10px;color:var(--muted);padding:9px 11px;font:600 12px Manrope;cursor:pointer;backdrop-filter:blur(12px);text-align:left}.studio-utility button:hover,.studio-utility button:focus-visible{color:var(--text);border-color:var(--line2);background:var(--panel2);outline:0}.studio-utility button b{color:var(--green);font:500 10px 'DM Mono';letter-spacing:.08em;text-transform:uppercase}.studio-drawer-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.48);opacity:0;pointer-events:none;transition:opacity .2s;z-index:55}.studio-drawer-backdrop.open{opacity:1;pointer-events:auto}.studio-drawer{position:fixed;top:14px;right:14px;bottom:14px;z-index:60;width:min(480px,calc(100vw - 28px));background:var(--panel);border:1px solid var(--line2);border-radius:18px;box-shadow:0 28px 90px rgba(0,0,0,.62);transform:translateX(calc(100% + 28px));transition:transform .22s ease;display:flex;flex-direction:column;overflow:hidden}.studio-drawer.open{transform:none}.studio-drawer-head{display:flex;align-items:center;justify-content:space-between;padding:18px 18px 13px;border-bottom:1px solid var(--line)}.studio-drawer-head strong{font:600 18px 'Playfair Display',serif}.studio-drawer-head span{display:block;color:var(--faint);font:10px 'DM Mono';letter-spacing:.09em;text-transform:uppercase;margin-top:3px}.studio-close{border:1px solid var(--line);background:transparent;color:var(--muted);width:32px;height:32px;border-radius:9px;cursor:pointer;font-size:17px}.studio-close:hover{color:var(--text);border-color:var(--line2)}.studio-tabs{display:flex;gap:5px;overflow:auto;padding:10px 12px;border-bottom:1px solid var(--line)}.studio-tabs button{flex:0 0 auto;border:1px solid transparent;background:transparent;color:var(--faint);border-radius:8px;padding:7px 9px;font:600 11px Manrope;cursor:pointer}.studio-tabs button.active,.studio-tabs button:hover{color:var(--text);background:var(--panel2);border-color:var(--line)}.studio-body{overflow:auto;min-height:0;padding:18px}.studio-card{border:1px solid var(--line);background:rgba(255,255,255,.018);border-radius:12px;padding:14px;margin-bottom:10px}.studio-card h3{margin:0 0 7px;font-size:13px}.studio-card p{margin:0;color:var(--muted);font-size:12px;line-height:1.55}.studio-muted{color:var(--faint);font-size:11px;line-height:1.5}.studio-row{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.studio-spacer{flex:1}.studio-btn{border:1px solid var(--line2);background:var(--panel2);color:var(--text);border-radius:9px;padding:8px 10px;font:600 11px Manrope;cursor:pointer}.studio-btn:hover{border-color:rgba(52,209,126,.55)}.studio-btn.primary{background:var(--green);color:#03140b;border-color:var(--green)}.studio-btn.danger{color:#ff9a9a}.studio-select,.studio-input,.studio-textarea{display:block;width:100%;border:1px solid var(--line);border-radius:9px;background:#080809;color:var(--text);padding:9px 10px;font:12px Manrope;outline:0}.studio-select:focus,.studio-input:focus,.studio-textarea:focus{border-color:rgba(52,209,126,.6)}.studio-textarea{font:12px/1.6 'DM Mono';min-height:160px;resize:vertical}.studio-label{display:block;color:var(--faint);font:10px 'DM Mono';letter-spacing:.08em;text-transform:uppercase;margin:13px 0 6px}.studio-list{display:grid;gap:6px}.studio-item{display:flex;align-items:center;justify-content:space-between;gap:10px;border:1px solid var(--line);border-radius:9px;padding:9px 10px}.studio-item button{border:0;background:transparent;color:var(--text);font:600 12px Manrope;text-align:left;cursor:pointer}.studio-item small{color:var(--faint);font:10px 'DM Mono'}.studio-kpi{font:24px 'Playfair Display',serif;color:var(--green)}.studio-toast{position:fixed;right:22px;bottom:22px;z-index:90;background:var(--panel2);border:1px solid var(--line2);color:var(--text);border-radius:10px;padding:10px 13px;font:12px Manrope;opacity:0;transform:translateY(8px);pointer-events:none;transition:opacity .18s,transform .18s}.studio-toast.show{opacity:1;transform:none}.light{--bg:#f5f7f3;--panel:#ffffff;--panel2:#edf2ed;--line:rgba(10,33,19,.1);--line2:rgba(10,33,19,.2);--text:#16221a;--muted:#5e6b62;--faint:rgba(22,34,26,.48);--green:#137b48;--green-dim:rgba(19,123,72,.12)}.light .studio-utility button{background:rgba(255,255,255,.9)}.light .message.assistant{color:#19261e}.light .composer{background:linear-gradient(transparent,rgba(245,247,243,.92) 26%)}.light .studio-select,.light .studio-input,.light .studio-textarea{background:#f8fbf8}@media(max-width:760px){.studio-utility{left:8px;right:8px;bottom:8px;width:auto;display:flex;overflow:auto}.studio-utility button{width:auto;flex:0 0 auto}.studio-utility button span{display:none}.studio-drawer{top:8px;right:8px;bottom:8px;width:calc(100vw - 16px)}}
</style><div class="studio-utility" aria-label="Studio workspace"><button data-studio-panel="workspace"><b>01</b><span>Workspace</span></button><button data-studio-panel="projects"><b>02</b><span>Projects & VFS</span></button><button data-studio-panel="agent"><b>03</b><span>Agent tools</span></button><button data-studio-panel="plugins"><b>04</b><span>Plugins & MCP</span></button><button data-studio-panel="usage"><b>05</b><span>Usage & settings</span></button></div><div class="studio-drawer-backdrop" id="studioBackdrop"></div><aside class="studio-drawer" id="studioDrawer" aria-hidden="true" role="dialog" aria-label="Studio workspace panel"><div class="studio-drawer-head"><div><strong id="studioPanelTitle">Workspace</strong><span>Stay in the conversation</span></div><button class="studio-close" id="studioClose" aria-label="Close panel">×</button></div><nav class="studio-tabs"><button data-studio-panel="workspace">Home</button><button data-studio-panel="projects">Projects</button><button data-studio-panel="agent">Agent</button><button data-studio-panel="plugins">Plugins</button><button data-studio-panel="usage">Usage</button><button data-studio-panel="account">Account</button></nav><div class="studio-body" id="studioBody"></div></aside><div id="studioToast" class="studio-toast" role="status"></div>`;
  const support = '<style>.studio-support{position:fixed;inset:0;z-index:100;background:rgba(0,0,0,.68);display:grid;place-items:center;padding:18px}.studio-support[hidden]{display:none}.studio-support-card{max-width:450px;background:var(--panel);border:1px solid var(--line2);border-radius:18px;padding:24px;box-shadow:0 28px 90px rgba(0,0,0,.6)}.studio-support-card h2{margin:0 0 10px;font:600 27px Playfair Display,serif}.studio-support-card p{color:var(--muted);line-height:1.65;font-size:13px}</style><div id="studioSupport" class="studio-support" hidden><div class="studio-support-card"><h2>You deserve support right now.</h2><p>If you may hurt yourself or feel unsafe, contact local emergency services now. In the U.S. or Canada, call or text <strong>988</strong>. Elsewhere, use your local crisis line or go to the nearest emergency department.</p><p>You can also reach someone you trust and move away from anything you could use to hurt yourself. You do not have to handle this moment alone.</p><button class="studio-btn primary" id="closeSupport">Close</button></div></div>';
  return page.replace('</body></html>', extra + support + '<script>' + workspaceScript() + '</script><script>' + supportScript() + '</script><script>' + chatGPTImageScript() + '</script><script>' + chatGPTUiScript() + '</script><script>' + chatGPTAgentScript() + '</script><script>' + browserVfsScript() + '</script></body></html>');
}

function chatGPTAgentScript(): string { return `(function(){const original=agentModel;agentModel=()=>selectedModel?selectedModel():original()})();`; }

function chatGPTImageScript(): string { return `(function(){
const original=renderMessage,IMAGE_DB='osaii-generated-images-v1',IMAGE_STORE='images';let imageDbPromise;
const imageMatch=value=>String(value||'').trim().match(/^!\\[Generated image\\]\\((data:image\\/[^;]+;base64,[A-Za-z0-9+/_=-]+|osaii-image:\\/\\/[A-Za-z0-9_-]+|https:\\/\\/[^)]+)\\)$/);
const imageId=()=>typeof crypto?.randomUUID==='function'?crypto.randomUUID().replaceAll('-',''):Date.now().toString(36)+Math.random().toString(36).slice(2);
const imageDb=()=>{if(imageDbPromise)return imageDbPromise;if(!('indexedDB'in window))return Promise.reject(new Error('Browser image storage is unavailable.'));imageDbPromise=new Promise((resolve,reject)=>{const request=indexedDB.open(IMAGE_DB,1);request.onupgradeneeded=()=>request.result.createObjectStore(IMAGE_STORE);request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error||new Error('Could not open browser image storage.'))});return imageDbPromise};
const putImage=(id,source)=>imageDb().then(db=>new Promise((resolve,reject)=>{const tx=db.transaction(IMAGE_STORE,'readwrite');tx.objectStore(IMAGE_STORE).put(source,id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('Could not save generated image.'))}));
const getImage=id=>imageDb().then(db=>new Promise((resolve,reject)=>{const tx=db.transaction(IMAGE_STORE,'readonly');const request=tx.objectStore(IMAGE_STORE).get(id);request.onsuccess=()=>resolve(typeof request.result==='string'?request.result:'');request.onerror=()=>reject(request.error||new Error('Could not load generated image.'))}));
const showImage=(el,source)=>{el.textContent='';const image=document.createElement('img');image.src=source;image.alt='Generated image';image.loading='lazy';image.style.maxWidth='100%';image.style.borderRadius='12px';image.style.display='block';image.onerror=()=>{if(image.isConnected)el.textContent='Generated image could not be loaded.'};el.append(image)};
const persistImage=async source=>{const id=imageId();await putImage(id,source);return 'osaii-image://'+id};window.__osaiiPersistGeneratedImage=persistImage;
renderMessage=(role,text,el)=>{const match=role==='assistant'&&imageMatch(text);if(!match){original(role,text,el);return}const source=match[1];if(!source.startsWith('osaii-image://')){showImage(el,source);return}el.textContent='Loading generated image…';getImage(source.slice('osaii-image://'.length)).then(value=>{if(value)showImage(el,value);else el.textContent='Generated image unavailable in this browser.'}).catch(()=>{el.textContent='Generated image unavailable in this browser.'})};
const persistChat=saveCurrentChat;let migratingLegacy=false;saveCurrentChat=()=>{const legacy=state.messages.filter(message=>message?.role==='assistant'&&imageMatch(message.content)?.[1].startsWith('data:image/'));if(!legacy.length)return persistChat();if(migratingLegacy)return;migratingLegacy=true;Promise.all(legacy.map(async message=>({message,ref:await persistImage(imageMatch(message.content)[1])}))).then(records=>{state.messages=state.messages.map(message=>{const record=records.find(item=>item.message===message);return record?{...message,content:'![Generated image]('+record.ref+')'}:message});persistChat()}).catch(()=>{console.warn('A generated image could not be saved for refresh.');persistChat()}).finally(()=>{migratingLegacy=false})};
const reopenChat=openChat;openChat=id=>{reopenChat(id);if(state.messages.some(message=>message?.role==='assistant'&&imageMatch(message.content)?.[1].startsWith('data:image/')))saveCurrentChat()};
})();`; }

function chatGPTUiScript(): string { return `(function(){const chatgpt=document.querySelector('#chatgptModel');if(!chatgpt)return;const baseFetch=window.fetch.bind(window);window.fetch=async(input,init={})=>{const url=String(input||'');if(url.includes('/studio/api/chat')&&!url.includes('/studio/api/chatgpt')&&typeof init.body==='string'){try{const body=JSON.parse(init.body);if(isChatGPTModel(body.model)){const model=chatGPTModelId(body.model);const headers=await chatGPTHeaders();if(model==='gpt-image-2'){const prompt=(Array.isArray(body.messages)?body.messages.filter(m=>m&&m.role==='user').at(-1)?.content:'')||'';const imageResponse=await baseFetch('/studio/api/chatgpt/images/generations',{...init,headers,body:JSON.stringify({model,prompt,response_format:'b64_json'})});const imageData=await imageResponse.json().catch(()=>({}));const item=imageData.data?.[0];const source=item?.b64_json?'data:image/png;base64,'+item.b64_json:item?.url;if(source){let durableSource=source;try{if(typeof window.__osaiiPersistGeneratedImage==='function')durableSource=await window.__osaiiPersistGeneratedImage(source)}catch(error){console.warn('Generated image persistence failed; this image may not survive refresh.',error)}return new Response(JSON.stringify({id:'chatcmpl-image',object:'chat.completion',choices:[{index:0,message:{role:'assistant',content:'![Generated image]('+durableSource+')'},finish_reason:'stop'}]}),{status:200,headers:{'content-type':'application/json'}})}return new Response(JSON.stringify(imageData),{status:imageResponse.status,headers:{'content-type':'application/json'}})}const next={...body,model};delete next.chat_template_kwargs;delete next.temperature;if(state.reasoning!=='none')next.reasoning_effort=state.reasoning==='xhigh'?'high':state.reasoning;else delete next.reasoning_effort;return baseFetch('/studio/api/chatgpt/chat/completions',{...init,headers,body:JSON.stringify(next)})}}catch{}}return baseFetch(input,init)}})();`; }

function supportScript(): string { return `(function(){const box=document.querySelector('#studioSupport');const close=document.querySelector('#closeSupport');if(close)close.onclick=()=>box.hidden=true;const oldFetch=window.fetch.bind(window);window.fetch=async function(input,init={}){let nextInit=init;const url=String(input||'');if(url.includes('/studio/api/chat')&&init&&typeof init.body==='string'&&window.__osaiiState){try{const data=JSON.parse(init.body);const hasToolContext=Array.isArray(data.messages)&&data.messages.some(m=>m&&((m.role==='tool')||m.tool_calls));if(data.surface==='agent'&&!hasToolContext&&Array.isArray(window.__osaiiState.messages)){const system=data.messages.find(m=>m&&m.role==='system');const history=window.__osaiiState.messages.map(m=>({role:m.role,content:m.content}));data.messages=system?[system,...history]:history;nextInit={...init,body:JSON.stringify(data)}}}catch{}}const response=await oldFetch(input,nextInit);const contentType=response.headers.get('content-type')||'';if(url.includes('/studio/api/')&&!contentType.toLowerCase().includes('text/event-stream')){try{const data=await response.clone().json();const code=data.error&&typeof data.error==='object'?data.error.code:data.code;if(code==='self_harm_support'&&box)box.hidden=false}catch{}}return response}})();`; }

function browserVfsScript(): string { return `(function(){
let root=null,files=new Map(),current='';
const body=()=>document.querySelector('#studioBody');
const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function walk(dir,prefix){for await(const item of dir.values()){const path=prefix+item.name;if(item.kind==='file')files.set(path,item);else await walk(item,path+'/')}}
async function loadFile(){const h=files.get(current),area=document.querySelector('#browserFileContent');if(!area)return;if(!h){area.value='';return}const f=await h.getFile();area.value=await f.text()}
function card(){return '<div class="studio-card" id="browserVfsCard"><div class="studio-row"><div><h3>Browser files</h3><p>Work directly in a local folder. Empty folders are supported and stay on this device.</p></div><span class="studio-spacer"></span><button class="studio-btn" id="browserPick">Choose folder</button></div><div id="browserFilesEmpty" class="studio-muted" style="margin-top:10px">No local folder selected.</div><select class="studio-select" id="browserFile" style="margin-top:10px;display:none"></select><input class="studio-input" id="browserNewPath" value="index.html" aria-label="New file name" style="margin-top:10px;display:none"><textarea class="studio-textarea" id="browserFileContent" placeholder="Start typing…" style="margin-top:9px;display:none"></textarea><button class="studio-btn primary" id="browserSave" style="margin-top:9px;display:none">Save local file</button></div>'}
function showEditor(){const names=Array.from(files.keys()).sort(),select=document.querySelector('#browserFile'),path=document.querySelector('#browserNewPath'),area=document.querySelector('#browserFileContent'),save=document.querySelector('#browserSave');select.innerHTML=names.map(x=>'<option>'+esc(x)+'</option>').join('');select.style.display=names.length?'block':'none';path.style.display=names.length?'none':'block';area.style.display='block';save.style.display='inline-block';current=names[0]||'index.html';select.value=current;path.value=current;return loadFile()}
async function bind(){const b=body();if(!b||!document.querySelector('#studioPanelTitle')||document.querySelector('#studioPanelTitle').textContent!=='Projects & VFS'||document.querySelector('#browserVfsCard'))return;if(!b.querySelector('.studio-card'))return;b.insertAdjacentHTML('afterbegin',card());document.querySelector('#browserPick').onclick=async()=>{if(!window.showDirectoryPicker){alert('Browser File System Access is not available in this browser.');return}try{root=await window.showDirectoryPicker({mode:'readwrite'});files.clear();await walk(root,'');document.querySelector('#browserFilesEmpty').textContent=files.size?files.size+' local files loaded.':'This folder is empty. Start with index.html below.';await showEditor()}catch(e){if(e.name!=='AbortError')alert(e.message)}};document.querySelector('#browserFile').onchange=async e=>{current=e.target.value;await loadFile()};document.querySelector('#browserSave').onclick=async()=>{if(!root)return;let h=files.get(current);if(!h){const name=document.querySelector('#browserNewPath').value.trim();if(!name||name.includes('/')||name.includes('\\\\')||name==='.'||name==='..'){alert('Use a file name such as index.html.');return}current=name;h=await root.getFileHandle(name,{create:true});files.set(name,h)}const w=await h.createWritable();await w.write(document.querySelector('#browserFileContent').value);await w.close();document.querySelector('#browserFilesEmpty').textContent='Saved locally at '+current+'. It was not uploaded.';await showEditor()}};
new MutationObserver(bind).observe(document.body,{subtree:true,childList:true});bind()
})();`; }

function workspaceScript(): string { return `
(function(){
const q=s=>document.querySelector(s), qa=s=>Array.from(document.querySelectorAll(s));
const studio={account:null,projects:[],active:null,files:[],file:'index.html',theme:localStorage.getItem('osaii.theme')||'dark',authMode:'login',createOpen:false};
const esc=x=>String(x==null?'':x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const toast=(text)=>{const el=q('#studioToast');el.textContent=text;el.classList.add('show');clearTimeout(studio.toastTimer);studio.toastTimer=setTimeout(()=>el.classList.remove('show'),2800)};
const apiErrorMessage=data=>{const value=data?.error??data?.message;if(typeof value==='string'&&value.trim())return value;if(value&&typeof value==='object'&&typeof value.message==='string')return value.message;try{if(value)return JSON.stringify(value)}catch{}return 'Studio request failed'};const api=async(path,options={})=>{const r=await fetch('/studio/api'+path,{credentials:'same-origin',...options,headers:{...(options.body?{'content-type':'application/json'}:{}),...(options.headers||{})}});const data=await r.json().catch(()=>({}));if(!r.ok)throw Object.assign(new Error(apiErrorMessage(data)),{status:r.status,data});return data};
function setTheme(){document.documentElement.classList.toggle('light',studio.theme==='light');localStorage.setItem('osaii.theme',studio.theme);const meta=document.querySelector('meta[name="theme-color"]');if(meta)meta.content=studio.theme==='light'?'#f5f7f3':'#000'}
async function loadSession(){try{studio.account=(await api('/auth/session')).account}catch{studio.account=null}return studio.account}
function openPanel(name){q('#studioDrawer').classList.add('open');q('#studioBackdrop').classList.add('open');q('#studioDrawer').setAttribute('aria-hidden','false');qa('[data-studio-panel]').forEach(b=>b.classList.toggle('active',b.dataset.studioPanel===name));q('#studioPanelTitle').textContent=name==='workspace'?'Workspace':name==='projects'?'Projects & VFS':name==='agent'?'Agent tools':name==='plugins'?'Plugins & MCP':name==='usage'?'Usage & settings':'OSAII Platform account';return renderPanel(name)}
function closePanel(){q('#studioDrawer').classList.remove('open');q('#studioBackdrop').classList.remove('open');q('#studioDrawer').setAttribute('aria-hidden','true')}
function requireAccount(){if(studio.account)return true;openPanel('account');toast('Sign in to unlock this workspace');return false}
async function renderPanel(name){const body=q('#studioBody');body.innerHTML='<div class="studio-muted">Loading…</div>';try{if(name==='workspace')body.innerHTML=workspaceView();if(name==='account')body.innerHTML=accountView();if(name==='projects'){await loadProjects();body.innerHTML=projectsView()}if(name==='agent')body.innerHTML=agentView();if(name==='plugins'){const p=await api('/plugins');body.innerHTML=pluginsView(p.plugins||[])}if(name==='usage'){if(studio.account){const [u,a]=await Promise.all([api('/usage'),api('/audit')]);body.innerHTML=usageView(u,a)}else body.textContent='Sign in to view usage. Guests can use standard Chat; Agent, Swarm, Advanced models, VFS, plugins, and publishing require an OSAII Platform account.'}bindPanel(name)}catch(e){body.innerHTML='<div class="studio-card"><h3>'+esc(e.message)+'</h3><p>That panel could not load. Try again.</p></div>'}}
function workspaceView(){return '<div class="studio-card"><h3>Keep the chat in focus</h3><p>Projects, tools, and publishing live here so the main conversation stays calm.</p></div><div class="studio-card"><div class="studio-row"><div><div class="studio-muted">ACCOUNT</div><strong>'+esc(studio.account?studio.account.display_name:'Guest mode')+'</strong></div><span class="studio-spacer"></span><button class="studio-btn" data-studio-panel="account">'+(studio.account?'Manage':'Sign in')+'</button></div></div><div class="studio-card"><h3>Current model</h3><p id="panelModel">'+esc(q('#modelLabel')?.textContent||'Fast')+'</p><div class="studio-row" style="margin-top:10px"><button class="studio-btn" data-studio-panel="agent">Open Agent tools</button><button class="studio-btn" data-studio-panel="projects">Open Projects</button></div></div><div class="studio-card"><div class="studio-row"><span class="studio-muted">APPEARANCE</span><span class="studio-spacer"></span><button class="studio-btn" id="themeToggle">'+(studio.theme==='light'?'Use dark mode':'Use light mode')+'</button></div></div>'}
async function loadProjects(){if(!studio.account)throw new Error('Sign in to manage projects and VFS.');studio.projects=(await api('/projects')).projects||[]}
async function createProjectFromForm(){const button=q('#createProject'),status=q('#projectCreateStatus');if(button)button.disabled=true;if(status)status.textContent='Creating…';try{const project=await api('/projects',{method:'POST',body:JSON.stringify({name:q('#newProjectName')?.value.trim()||'My workspace',storage_mode:q('#newProjectStorage')?.value||'cloud',permission_mode:q('#newProjectPermission')?.value||'reads'})});await loadProjects();studio.active=studio.projects.find(p=>p.id===project.id)||project;studio.createOpen=false;toast('Project ready');await openProject(project.id)}catch(e){if(status)status.textContent=e.message;if(button)button.disabled=false}}
async function ensureActiveProject(){if(studio.active?.storage_mode==='cloud'&&studio.active.status!=='disabled')return studio.active;await loadProjects();studio.active=studio.projects.find(p=>p.storage_mode==='cloud'&&p.status!=='disabled')||null;if(!studio.active){const project=await api('/projects',{method:'POST',body:JSON.stringify({name:'Agent workspace',storage_mode:'cloud',permission_mode:'reads'})});await loadProjects();studio.active=studio.projects.find(p=>p.id===project.id)||project;toast('Agent workspace created')}return studio.active}
function toolPermission(tool){const selected=q('#permissions')?.value;if(selected==='deny')return 'denied';if(selected==='full')return 'granted';if(selected==='low'&&(tool==='search'||tool==='vfs.read'))return 'granted';if(selected==='ask')return confirm('Allow '+tool+' for this request?')?'granted':'denied';const mode=studio.active?.permission_mode||'confirm';if(mode==='deny')return 'denied';if(mode==='allow'||(mode==='reads'&&(tool==='search'||tool==='vfs.read')))return 'granted';return confirm('Allow '+tool+' for this request?')?'granted':'denied'}
function projectCreator(){return '<div class="studio-card"><h3>Create a project</h3><p>A cloud workspace works with Agent immediately. Browser folders remain local.</p><label class="studio-label">Name</label><input class="studio-input" id="newProjectName" placeholder="My workspace" autocomplete="off"><div class="studio-row" style="margin-top:9px"><select class="studio-select" id="newProjectStorage" style="flex:1"><option value="cloud">Cloud workspace</option><option value="browser">Browser folder</option></select><select class="studio-select" id="newProjectPermission" style="flex:1"><option value="reads">Auto-run reads</option><option value="confirm">Ask every time</option><option value="allow">Auto-run all tools</option><option value="deny">Agent tools off</option></select></div><div class="studio-row" style="margin-top:10px"><button class="studio-btn primary" id="createProject">Create project</button><button class="studio-btn" id="cancelProject">Cancel</button><span class="studio-muted" id="projectCreateStatus"></span></div></div>'}
function permissionLabel(value){return value==='reads'?'auto-run reads':value==='allow'?'auto-run all':value==='deny'?'tools off':'ask every time'}
function projectsView(){if(!studio.account)return '<div class="studio-card"><h3>Projects are account workspaces</h3><p>Sign in to create a project, use Cloud VFS, and publish a static site.</p><button class="studio-btn primary" data-studio-panel="account" style="margin-top:12px">Sign in</button></div>';let html='<div class="studio-row" style="margin-bottom:12px"><div><div class="studio-muted">YOUR PROJECTS</div><strong>'+studio.projects.length+' workspace'+(studio.projects.length===1?'':'s')+'</strong></div><span class="studio-spacer"></span><button class="studio-btn primary" id="newProject">+ New project</button></div>';if(studio.createOpen||!studio.projects.length)html+=projectCreator();html+='<div class="studio-list">'+(studio.projects.length?studio.projects.map(p=>'<div class="studio-item"><button data-project="'+esc(p.id)+'"><b>'+esc(p.name)+'</b><br><small>'+esc(p.storage_mode)+' VFS · '+esc(p.status)+'</small></button><small>'+esc(permissionLabel(p.permission_mode))+'</small></div>').join(''):'<div class="studio-card"><p>Your first project can be an empty workspace. No starter files required.</p></div>')+'</div>';if(studio.active)html+=projectEditor();return html}
function projectEditor(){const p=studio.active;return '<div class="studio-card" style="margin-top:14px"><div class="studio-row"><div><h3>'+esc(p.name)+'</h3><p>'+esc(p.storage_mode)+' VFS · '+esc(permissionLabel(p.permission_mode))+'</p></div><span class="studio-spacer"></span><button class="studio-btn danger" id="disableProject">Disable</button></div><label class="studio-label">Agent permission</label><select class="studio-select" id="projectPermission"><option value="reads"'+(p.permission_mode==='reads'?' selected':'')+'>Auto-run reads and search; ask before changes</option><option value="confirm"'+(p.permission_mode==='confirm'?' selected':'')+'>Ask before every tool</option><option value="allow"'+(p.permission_mode==='allow'?' selected':'')+'>Auto-run all tools</option><option value="deny"'+(p.permission_mode==='deny'?' selected':'')+'>Turn Agent tools off</option></select><label class="studio-label">File</label><select class="studio-select" id="projectFile">'+studio.files.map(f=>'<option value="'+esc(f.path)+'"'+(f.path===studio.file?' selected':'')+'>'+esc(f.path)+'</option>').join('')+'</select><textarea class="studio-textarea" id="projectContent" placeholder="Empty file — start typing"></textarea><div class="studio-row" style="margin-top:9px"><button class="studio-btn" id="saveProjectFile">Save file</button><span class="studio-spacer"></span><button class="studio-btn primary" id="publishProject">Publish static site</button></div><p class="studio-muted" id="projectStatus" style="margin-top:9px">Empty projects are ready to use. Saving creates index.html.</p></div>'}
async function openProject(id){studio.active=studio.projects.find(p=>p.id===id)||null;studio.file='index.html';if(!studio.active)return;studio.files=(await api('/projects/'+encodeURIComponent(id)+'/files')).files||[];if(!studio.files.some(f=>f.path==='index.html'))studio.files.unshift({path:'index.html'});await openPanel('projects');await loadProjectFile()}
async function loadProjectFile(){if(!studio.active)return;const r=await fetch('/studio/api/projects/'+encodeURIComponent(studio.active.id)+'/files/'+encodeURIComponent(studio.file),{credentials:'same-origin'});q('#projectContent').value=r.ok?await r.text():''}
async function saveProjectFile(){const path=q('#projectFile').value||'index.html';const response=await fetch('/studio/api/projects/'+encodeURIComponent(studio.active.id)+'/files/'+encodeURIComponent(path),{method:'PUT',credentials:'same-origin',headers:{'content-type':path.endsWith('.html')?'text/html':path.endsWith('.css')?'text/css':path.endsWith('.js')?'text/javascript':'text/plain'},body:q('#projectContent').value});if(!response.ok){const data=await response.json().catch(()=>({}));throw new Error(data.message||data.error||'File could not be saved')}toast('Saved '+path);studio.file=path;studio.files=(await api('/projects/'+studio.active.id+'/files')).files||[];renderPanel('projects')}
function agentView(){if(!studio.account)return '<div class="studio-card"><h3>Agent mode needs an account</h3><p>Chat stays available to guests. Sign in for VFS, search, MCP, and deploy actions.</p><button class="studio-btn primary" data-studio-panel="account" style="margin-top:12px">Sign in</button></div>';return '<div class="studio-card"><h3>Agent is ready in chat</h3><p>Choose Agent in the composer and describe the outcome. OSS Studio automatically uses a cloud workspace; reads run quietly and changes follow its permission setting.</p></div><div class="studio-card"><div class="studio-row"><button class="studio-btn" data-tool="search">Search web</button><button class="studio-btn" data-tool="vfs.read">Read file</button><button class="studio-btn" data-tool="vfs.write">Write file</button><button class="studio-btn" data-tool="mcp.execute">Run MCP</button><button class="studio-btn primary" data-tool="deploy">Publish</button></div><p class="studio-muted" id="toolStatus" style="margin-top:10px">These shortcuts use your active workspace, or create one automatically.</p></div><div class="studio-card"><h3>Reasoning</h3><p>'+esc(q('#reasoning')?.textContent||'Reasoning: Light')+'</p><p class="studio-muted">Use the control in the composer to cycle None, Light, Medium, and Max.</p></div>'}
async function runTool(tool){try{await ensureActiveProject()}catch(e){toast(e.message);return}let query='';if(tool==='search')query=prompt('Search the web for:','');if(tool==='vfs.read')query=prompt('Project-relative file path:','index.html');if(tool==='vfs.write'){const path=prompt('Project-relative file path:','index.html');if(path===null)return;const content=prompt('File contents:', '');query=JSON.stringify({path,content})}if(query===null)return;const permission=toolPermission(tool);if(permission==='denied'){toast(studio.active.permission_mode==='deny'?'Agent tools are off for this project':'Action cancelled');return}try{const result=await api('/projects/'+encodeURIComponent(studio.active.id)+'/tools',{method:'POST',body:JSON.stringify({tool,query,permission})});q('#toolStatus').textContent=result.message||result.response||'Tool completed.';toast('Tool completed')}catch(e){q('#toolStatus').textContent=e.message;toast(e.message)}}
function pluginsView(items){if(!studio.account)return '<div class="studio-card"><h3>Integrations need an account</h3><button class="studio-btn primary" data-studio-panel="account" style="margin-top:12px">Sign in</button></div>';return '<div class="studio-card"><h3>Add an integration</h3><p>Only HTTPS MCP endpoints are accepted. Tool calls remain subject to project permission.</p><label class="studio-label">Type</label><select class="studio-select" id="pluginKind"><option value="mcp">MCP server</option><option value="skill">Skill manifest</option></select><label class="studio-label">Name</label><input class="studio-input" id="pluginName" placeholder="Search tools"><label class="studio-label">MCP URL</label><input class="studio-input" id="pluginEndpoint" placeholder="https://example.com/mcp"><button class="studio-btn primary" id="addPlugin" style="margin-top:10px">Add integration</button></div><div class="studio-card"><h3>Configured</h3><div class="studio-list">'+(items.length?items.map(p=>'<div class="studio-item"><span><b>'+esc(p.name)+'</b><br><small>'+esc(p.kind)+' · '+esc(p.endpoint||'local manifest')+'</small></span><small>'+ (p.enabled?'ON':'OFF')+'</small></div>').join(''):'<p>No integrations configured.</p>')+'</div></div>'}
function usageView(u,a){const rows=u.usage||[];const total=rows.reduce((n,x)=>n+Number(x.count||0),0);return '<div class="studio-card"><div class="studio-muted">RECORDED ACTIVITY · 30 DAYS</div><div class="studio-kpi">'+total+'</div><p>Requests, tools, files, deployments, and safety decisions recorded for this account.</p></div><div class="studio-card"><h3>Visible limits</h3><p>Studio accounts use a high-throughput fair-use envelope: up to 600 requests/minute, 6,000/hour, and 20,000/day across Chat, Agent, and Advanced models. Guests can use standard Chat with a smaller anti-abuse allowance. These limits are shared across your account and protect availability during traffic spikes.</p></div><div class="studio-card"><h3>Recent audit</h3><div class="studio-list">'+((a.events||[]).slice(0,12).map(x=>'<div class="studio-item"><span>'+esc(x.kind)+'<br><small>'+esc(x.detail)+'</small></span><small>'+esc(x.created_at)+'</small></div>').join('')||'<p>No actions recorded yet.</p>')+'</div></div>'}
function accountView(){return '<div class="studio-card"><h3>'+(studio.account?'Signed in':'OSAII Platform account')+'</h3><p>'+(studio.account?'Signed in as '+esc(studio.account.email)+'. Advanced models and the workspace are unlocked.':'Chat is available as a guest. Sign in for Agent, Swarm, Advanced models, VFS, plugins, and publishing.')+'</p>'+(studio.account?'<button class="studio-btn" id="signout" style="margin-top:12px">Sign out</button>':'<label class="studio-label">Email</label><input class="studio-input" id="authEmail" type="email" placeholder="you@example.com"><label class="studio-label">Password</label><input class="studio-input" id="authPassword" type="password" placeholder="8+ characters"><label class="studio-label" id="authNameLabel">Display name (for new accounts)</label><input class="studio-input" id="authName" placeholder="Your name"><div class="studio-row" style="margin-top:12px"><button class="studio-btn primary" id="authSubmit">Sign in</button><button class="studio-btn" id="authSwitch">Create account</button></div><p class="studio-muted" id="authStatus" style="margin-top:9px"></p>')+'</div><div class="studio-card"><div class="studio-row"><span class="studio-muted">THEME</span><span class="studio-spacer"></span><button class="studio-btn" id="themeToggle">'+(studio.theme==='light'?'Use dark mode':'Use light mode')+'</button></div></div>'}
async function authSubmit(){const email=q('#authEmail').value.trim(),password=q('#authPassword').value,name=q('#authName')?.value.trim()||'';const signup=studio.authMode==='signup';try{const d=await api('/auth/'+(signup?'signup':'login'),{method:'POST',body:JSON.stringify(signup?{email,password,display_name:name}:{email,password})});studio.account=d.account;toast('Signed in');if(window.loadAccess)window.loadAccess();openPanel('workspace')}catch(e){q('#authStatus').textContent=e.message}}
function bindPanel(name){qa('[data-studio-panel]').forEach(b=>b.onclick=()=>openPanel(b.dataset.studioPanel));q('#themeToggle')?.addEventListener('click',()=>{studio.theme=studio.theme==='light'?'dark':'light';setTheme();renderPanel(name)});q('#newProject')?.addEventListener('click',()=>{studio.createOpen=true;renderPanel('projects').then(()=>q('#newProjectName')?.focus())});q('#cancelProject')?.addEventListener('click',()=>{studio.createOpen=false;renderPanel('projects')});q('#createProject')?.addEventListener('click',createProjectFromForm);q('#newProjectName')?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();createProjectFromForm()}});qa('[data-project]').forEach(b=>b.onclick=()=>openProject(b.dataset.project));q('#projectFile')?.addEventListener('change',async e=>{studio.file=e.target.value;await loadProjectFile()});q('#saveProjectFile')?.addEventListener('click',()=>saveProjectFile().catch(e=>toast(e.message)));q('#publishProject')?.addEventListener('click',async()=>{try{const d=await api('/projects/'+studio.active.id+'/deploy',{method:'POST',body:'{}'});toast(d.message||'Static site published');q('#projectStatus').textContent='Published at '+d.url}catch(e){toast(e.message)}});q('#disableProject')?.addEventListener('click',async()=>{await api('/projects/'+studio.active.id,{method:'PATCH',body:JSON.stringify({status:'disabled'})});studio.active=null;toast('Project disabled');renderPanel('projects')});q('#projectPermission')?.addEventListener('change',async e=>{await api('/projects/'+studio.active.id,{method:'PATCH',body:JSON.stringify({permission_mode:e.target.value})});studio.active.permission_mode=e.target.value;toast('Permission updated')});qa('[data-tool]').forEach(b=>b.onclick=()=>runTool(b.dataset.tool));q('#addPlugin')?.addEventListener('click',async()=>{try{await api('/plugins',{method:'POST',body:JSON.stringify({kind:q('#pluginKind').value,name:q('#pluginName').value,endpoint:q('#pluginEndpoint').value})});toast('Integration added');renderPanel('plugins')}catch(e){toast(e.message)}});q('#authSubmit')?.addEventListener('click',authSubmit);q('#authSwitch')?.addEventListener('click',()=>{studio.authMode=studio.authMode==='signup'?'login':'signup';renderPanel('account')});q('#signout')?.addEventListener('click',async()=>{await fetch('/studio/api/auth/logout',{method:'POST'});studio.account=null;studio.active=null;toast('Signed out');if(window.loadAccess)window.loadAccess();renderPanel('account')})}
function agentModel(){const advanced=q('#advancedModel');if(advanced&&!advanced.classList.contains('hidden'))return advanced.value;return q('#speed')?.textContent==='Smart'?'poolside/laguna-s-2.1':'poolside/laguna-xs-2.1'}
function agentEffort(){const text=q('#reasoning')?.textContent||'';return text.endsWith('None')?'none':text.endsWith('Medium')?'medium':text.endsWith('Max')?'xhigh':'low'}function agentControls(model){const controls={chat_template_kwargs:{enable_thinking:agentEffort()!=='none'}};if(model.startsWith('logfare/'))controls.reasoning={effort:agentEffort()};return controls}
async function streamedAgentChat(payload,activity){const requestBody={...payload,stream:true,stream_options:{include_usage:true},parallel_tool_calls:true};const response=await fetch('/studio/api/chat',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json',accept:'text/event-stream'},body:JSON.stringify(requestBody)});if(!response.ok){const data=await response.json().catch(()=>({}));const failure=new Error(readableError(data));failure.code=data?.error?.code||data?.code||'';failure.status=response.status;throw failure}const contentType=response.headers.get('content-type')||'';if(!contentType.toLowerCase().includes('text/event-stream')){const data=await response.json().catch(()=>({}));for(const call of data.choices?.[0]?.message?.tool_calls||[]){const name=call.function?.name||'tool';activity?.args(name,call.function?.arguments||'')}return data}if(!response.body)return{choices:[{message:{role:'assistant',content:'No response returned.'}}]};const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='',raw='',content='',reasoning='',sawSse=false;const calls=new Map();const appendToolDelta=part=>{const index=Number.isFinite(part?.index)?part.index:0;const current=calls.get(index)||{id:'',type:'function',function:{name:'',arguments:''}};if(part?.id&&!current.id)current.id=part.id;if(part?.type)current.type=part.type;const fn=part?.function||{};if(typeof fn.name==='string'&&fn.name){current.function.name+=fn.name;activity?.call(current.function.name)}if(typeof fn.arguments==='string'&&fn.arguments){current.function.arguments+=fn.arguments;activity?.args(current.function.name||'tool',current.function.arguments)}calls.set(index,current)};const consumeLine=line=>{const text=line.trim();if(!text.startsWith('data:'))return;sawSse=true;const payloadText=text.slice(5).trim();if(payloadText==='[DONE]')return;let chunk;try{chunk=JSON.parse(payloadText)}catch{return}if(chunk.error)throw new Error(readableError(chunk,'Upstream provider error'));const delta=chunk.choices?.[0]?.delta||{};if(typeof delta.content==='string')content+=delta.content;if(typeof delta.reasoning_content==='string')reasoning+=delta.reasoning_content;for(const part of Array.isArray(delta.tool_calls)?delta.tool_calls:delta.function_call?[{index:0,function:delta.function_call}]:[])appendToolDelta(part)};const flush=()=>{let split;while((split=buffer.indexOf('\\n'))>=0){consumeLine(buffer.slice(0,split));buffer=buffer.slice(split+1)}};while(true){const {done,value}=await reader.read();if(done)break;const text=decoder.decode(value,{stream:true});raw+=text;buffer+=text;flush()}const tail=decoder.decode();if(tail){raw+=tail;buffer+=tail}flush();if(buffer.trim())consumeLine(buffer);if(!sawSse&&raw.trim()){try{const data=JSON.parse(raw);for(const call of data.choices?.[0]?.message?.tool_calls||[]){const name=call.function?.name||'tool';activity?.args(name,call.function?.arguments||'')}return data}catch{}}const toolCalls=Array.from(calls.values());return{choices:[{message:{role:'assistant',content:content||(!toolCalls.length&&reasoning?reasoningBudgetNotice():null),reasoning_content:reasoning||undefined,tool_calls:toolCalls}}]}}
async function resilientAgentChat(payload,activity){let last='',lastCode='';for(let attempt=0;attempt<3;attempt++){try{return await streamedAgentChat(payload,activity)}catch(e){last=e.message||'The model request failed';lastCode=e.code||'';if(attempt<2){activity?.phase(lastCode==='safety_filter_unavailable'?'Safety screening unavailable; retrying…':'Model request failed; retrying…');await new Promise(resolve=>setTimeout(resolve,350*(attempt+1)))}}}const message=lastCode==='safety_filter_unavailable'?'Safety screening is temporarily unavailable. No model request was sent; please retry.':'I could not reach the model after three attempts. No tool action was completed. Last error: '+last;return{choices:[{message:{content:message}}],agent_error:last,agent_error_code:lastCode}}
function cleanAgentText(text){return String(text||'').replace(/\\r\\n?/g,'\\n').replace(/[ \\t]+\\n/g,'\\n').replace(/\\n{3,}/g,'\\n\\n').trim()}
function normalizeAgentToolResult(tool,result){if(result==null)return{ok:true,message:tool+' completed successfully; no output was returned.'};if(!result.error&&['vfs.write','deploy','mcp.execute'].includes(tool)&&!result.message&&!result.response&&!result.content)return{...result,ok:true,message:tool+' completed successfully; no output was returned.'};return result}
async function agentRun(){if(!requireAccount())return;const input=q('#prompt').value.trim();if(!input)return;try{await ensureActiveProject()}catch(e){toast(e.message);return}q('#prompt').value='';q('#welcome')?.classList.add('hidden');const add=window.addMessage;if(!add)return;add('user',input);state.messages.push({role:'user',content:input});saveCurrentChat();let reply=add('assistant','',{typing:true}),activity=liveActivity;const messages=[{role:'system',content:'You are a practical task agent working in a project workspace. Use tools when they materially help. Reads and search may run automatically; respect tool denials, and explain the useful result rather than narrating routine steps. A successful tool call is progress: continue the task and do not stop just because a write returned successfully. A failed or denied tool must be included in the next model turn as explicit recovery context; explain the failure and choose another approach. Never perform a read solely to verify a successful write.'},{role:'user',content:input}];const tools=[{type:'function',function:{name:'web_search',description:'Search the public web.',parameters:{type:'object',properties:{query:{type:'string'}},required:['query']}}},{type:'function',function:{name:'vfs_read',description:'Read a project file.',parameters:{type:'object',properties:{path:{type:'string'}},required:['path']}}},{type:'function',function:{name:'vfs_write',description:'Write a project file.',parameters:{type:'object',properties:{path:{type:'string'},content:{type:'string'}},required:['path','content']}}},{type:'function',function:{name:'deploy_site',description:'Publish the project as a static site.',parameters:{type:'object',properties:{},additionalProperties:false}}},{type:'function',function:{name:'unknown_tool',description:'Fallback for an invalid function name; do not use unless another tool name fails validation.',parameters:{type:'object',properties:{},additionalProperties:true}}}];const toolAliases={web_search:'search',search:'search',vfs_read:'vfs.read','vfs.read':'vfs.read',vfs_write:'vfs.write','vfs.write':'vfs.write',deploy_site:'deploy',deploy:'deploy'};const modelToolNames={search:'web_search','vfs.read':'vfs_read','vfs.write':'vfs_write',deploy:'deploy_site'};try{for(let turn=0;turn<8;turn++){const response=await resilientAgentChat({model:agentModel(),surface:'agent',messages,tools,...agentControls(agentModel())},activity);const msg=response.choices?.[0]?.message||{};if(!msg.tool_calls?.length){activity?.finish();reply.textContent='';const finalText=cleanAgentText(msg.content||'No response returned.');if(window.renderMessage)window.renderMessage('assistant',finalText,reply);else reply.textContent=finalText;state.messages.push({role:'assistant',content:finalText});saveCurrentChat();break}const modelCalls=msg.tool_calls.map(call=>{const internal=toolAliases[call.function?.name]||'unknown_tool';return{...call,function:{...call.function,name:modelToolNames[internal]||'unknown_tool'}}});messages.push({role:'assistant',content:msg.content||null,tool_calls:modelCalls});for(const call of msg.tool_calls){let args={};try{args=JSON.parse(call.function?.arguments||'{}')}catch{}let rawTool=call.function?.name||'',tool=toolAliases[rawTool]||'unknown_tool';let query=tool==='search'?String(args.query||''):tool==='vfs.read'?String(args.path||''):tool==='vfs.write'?JSON.stringify({path:args.path,content:args.content}):'';activity?.call(tool);const permission=toolPermission(tool);let result;if(permission==='denied'){result={error:studio.active.permission_mode==='deny'?'Agent tools are disabled for this project.':'The user declined this action.'};activity?.complete(tool,'blocked')}else{try{result=await api('/projects/'+studio.active.id+'/tools',{method:'POST',body:JSON.stringify({tool,query,permission})})}catch(e){result={error:e.message}}result=normalizeAgentToolResult(tool,result);activity?.complete(tool,result?.error?'failed':'ok');}messages.push({role:'tool',tool_call_id:call.id||'agent-tool-'+turn,content:JSON.stringify(result)||'Tool completed successfully; no output was returned.'});reply.textContent=permission==='denied'?'Action skipped…':'Used '+tool+'…'}}}catch(e){activity?.finish();const failure='Could not complete that: '+cleanAgentText(e.message);reply.textContent=failure;state.messages.push({role:'assistant',content:failure});saveCurrentChat()}}
async function swarmRun(){if(!requireAccount())return;const choice=permissionSelect?.value||'low';if(choice==='deny'){toast('Agent Swarm is blocked by Permissions: Deny All');return}if(choice==='ask'&&!confirm('Allow Agent Swarm to launch 300 researchers?'))return;const input=q('#prompt').value.trim();if(!input)return;q('#prompt').value='';q('#welcome')?.classList.add('hidden');const add=window.addMessage;if(!add)return;add('user',input);const reply=add('assistant','Launching 300 fast researchers…',{typing:true}),activity=liveActivity;activity?.phase('Launching 300 researchers…');try{const data=await api('/swarm',{method:'POST',body:JSON.stringify({prompt:input})});activity?.complete('300 researcher passes');activity?.phase('Delegator synthesis complete');reply.textContent='';if(window.renderMessage)window.renderMessage('assistant',data.choices?.[0]?.message?.content||'The swarm returned no synthesis.',reply);else reply.textContent=data.choices?.[0]?.message?.content||'The swarm returned no synthesis.';activity?.finish()}catch(e){activity?.complete('Swarm request',false);activity?.finish();reply.textContent='Swarm could not complete: '+e.message}}
qa('[data-studio-panel]').forEach(b=>b.onclick=()=>openPanel(b.dataset.studioPanel));q('#studioClose').onclick=closePanel;q('#studioBackdrop').onclick=closePanel;document.addEventListener('keydown',e=>{if(e.key==='Escape')closePanel()});setTheme();document.title='OSS Studio';if(q('.logo'))q('.logo').textContent='OSS Studio';loadSession();
const sendButton=q('#send'),composer=q('#prompt');if(sendButton&&composer){sendButton.addEventListener('click',e=>{const mode=qa('[data-mode].active')[0]?.dataset.mode;if(mode==='agent'){e.preventDefault();e.stopImmediatePropagation();agentRun()}else if(mode==='swarm'){e.preventDefault();e.stopImmediatePropagation();swarmRun()}},true);composer.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){const mode=qa('[data-mode].active')[0]?.dataset.mode;if(mode==='agent'||mode==='swarm'){e.preventDefault();e.stopImmediatePropagation();mode==='agent'?agentRun():swarmRun()}}},true)}
})();` }

function appScript(): string { return `
const q=s=>document.querySelector(s),qa=s=>Array.from(document.querySelectorAll(s));
const chatGPTModelSelect=document.createElement('select');chatGPTModelSelect.id='chatgptModel';chatGPTModelSelect.className='mode hidden';chatGPTModelSelect.setAttribute('aria-label','ChatGPT Codex model');document.querySelector('#reasoning')?.parentElement?.insertBefore(chatGPTModelSelect,document.querySelector('#reasoning'));const advancedModelSelect=document.querySelector('#advancedModel');if(advancedModelSelect){[['logfare/minimax-m3','MiniMax M3'],['logfare/deepseek-v4-pro','DeepSeek V4 Pro']].forEach(([value,label])=>{if(!advancedModelSelect.querySelector('option[value="'+value+'"]')){const option=document.createElement('option');option.value=value;option.textContent=label;advancedModelSelect.append(option)}})}const chatGPTFamilyOption=document.querySelector('#chatgptFamily');if(chatGPTFamilyOption){chatGPTFamilyOption.hidden=false;chatGPTFamilyOption.disabled=true;chatGPTFamilyOption.textContent='ChatGPT / Codex (sign in)'}
let chatGPTSdkPromise;let chatGPTModels=[];const chatGPTInstallHint='Install the Sign in with ChatGPT extension, then try again.';const isChatGPTModel=model=>typeof model==='string'&&model.startsWith('chatgpt/');const chatGPTModelId=model=>isChatGPTModel(model)?model.slice(8):model;async function chatGPTSdk(){if(!chatGPTSdkPromise)chatGPTSdkPromise=import('https://esm.sh/@openai-oauth/web@2.0.0');return chatGPTSdkPromise}async function chatGPTHeaders(){const sdk=await chatGPTSdk();return sdk.openaiAuthHeaders({headers:{'content-type':'application/json'}})}function updateChatGPTAccountUi(){const button=document.querySelector('#chatgptLogin'),status=document.querySelector('#chatgptAuthStatus');if(!button)return;if(state.chatgptSignedIn){button.textContent='Disconnect ChatGPT';status.textContent='Connected · '+chatGPTModels.length+' Codex models available, including image generation.'}else{button.textContent='Sign in with ChatGPT';status.textContent='Bring your own ChatGPT/Codex access. Your OAuth session stays encrypted in this browser.'}}async function loadChatGPTAccess(){try{const sdk=await chatGPTSdk();const session=await sdk.getSession({refresh:true});state.chatgptSignedIn=!!session;if(session){const headers=await sdk.openaiAuthHeaders();const response=await fetch('/studio/api/chatgpt/models',{headers});const data=await response.json();chatGPTModels=Array.isArray(data.data)?data.data:[];chatGPTModelSelect.innerHTML=chatGPTModels.map(model=>'<option value="chatgpt/'+escapeHtml(model.id)+'">'+escapeHtml(model.name||model.id)+'</option>').join('');state.chatgptModel=chatGPTModels[0]?.id||'gpt-5.4-mini'}else{chatGPTModels=[];chatGPTModelSelect.innerHTML=''}renderModelControls();updateChatGPTAccountUi()}catch(error){state.chatgptSignedIn=false;updateChatGPTAccountUi();if(document.querySelector('#chatgptAuthStatus'))document.querySelector('#chatgptAuthStatus').textContent=error?.message||'ChatGPT sign-in is unavailable in this browser.'}}async function signInChatGPT(){try{const sdk=await chatGPTSdk();const result=await sdk.startLogin({callbackPath:'/studio/oauth/callback',returnTo:'/studio'});if(result?.status==='needs-extension'){const status=document.querySelector('#chatgptAuthStatus');if(status){status.innerHTML='<a href="'+escapeHtml(result.installUrl)+'" target="_blank" rel="noreferrer">'+chatGPTInstallHint+'</a>'}return}}catch(error){const status=document.querySelector('#chatgptAuthStatus');if(status)status.textContent=error?.message||'ChatGPT sign-in could not start.'}}async function disconnectChatGPT(){try{const sdk=await chatGPTSdk();await sdk.logout()}finally{state.chatgptSignedIn=false;chatGPTModels=[];chatGPTModelSelect.innerHTML='';state.tier='fast';renderModelControls();updateChatGPTAccountUi()}}setTimeout(loadChatGPTAccess,0);
function ensureChatGPTAccountCard(){const body=document.querySelector('#studioBody');if(!body||document.querySelector('#chatgptLogin'))return;const card=document.createElement('div');card.className='studio-card';card.innerHTML='<h3>ChatGPT / Codex</h3><p>Use your own ChatGPT account for its Codex model catalog and GPT Image generation. Your token stays in this browser and is only sent with requests you start.</p><div class="studio-row" style="margin-top:12px"><button class="studio-btn primary" id="chatgptLogin">Sign in with ChatGPT</button></div><p class="studio-muted" id="chatgptAuthStatus" style="margin-top:9px"></p>';body.append(card);document.querySelector('#chatgptLogin').onclick=()=>state.chatgptSignedIn?disconnectChatGPT():signInChatGPT();updateChatGPTAccountUi()}setInterval(()=>{if(document.querySelector('#studioPanelTitle')?.textContent==='OSAII Platform account')ensureChatGPTAccountCard()},250);
const studioLayoutStyle=document.createElement('style');studioLayoutStyle.textContent='html,body{height:100%;overflow:hidden}.shell{height:100dvh;min-height:0}.side{height:100dvh;overflow:hidden}.main{height:100dvh;min-height:0;overflow:hidden}.feed{flex:1;min-height:0;overflow-y:auto;overscroll-behavior:contain;scroll-behavior:auto;scrollbar-gutter:stable;align-content:start;align-items:start;grid-auto-rows:max-content}.feed>.message{align-self:start}.welcome{flex:none}.composer{flex:none}.follow-stream{position:fixed;right:28px;bottom:124px;z-index:40;border:1px solid rgba(52,209,126,.48);background:rgba(11,11,12,.94);backdrop-filter:blur(12px);color:#dff7ea;border-radius:999px;padding:9px 14px;font:600 12px Manrope;box-shadow:0 12px 32px rgba(0,0,0,.4);cursor:pointer}.follow-stream:hover,.follow-stream:focus-visible{background:#173d2b;color:#fff;outline:2px solid rgba(52,209,126,.4);outline-offset:2px}.light .follow-stream{background:rgba(255,255,255,.96);color:#145b39}@media(max-width:760px){.follow-stream{right:18px;bottom:106px}}';document.head.append(studioLayoutStyle);const followStream=document.createElement('button');followStream.id='followStream';followStream.className='follow-stream';followStream.type='button';followStream.textContent='↓ Follow live response';followStream.hidden=true;followStream.setAttribute('aria-label','Follow the live response');document.body.append(followStream);
const studioAgentStyle=document.createElement('style');studioAgentStyle.textContent='.permission-select{display:none;min-width:132px}.permission-select.visible{display:block}.agent-activity{grid-column:1/-1;border:1px solid rgba(52,209,126,.18);background:rgba(52,209,126,.045);border-radius:12px;padding:9px 12px;font:12px Manrope;color:var(--muted)}.agent-activity[hidden]{display:none}.agent-activity-head{display:flex;justify-content:space-between;gap:12px;color:var(--text);font-weight:700}.agent-activity-label{color:var(--green);font:10px DM Mono;letter-spacing:.08em;text-transform:uppercase}.agent-activity-current{margin-top:7px;color:var(--text);overflow-wrap:anywhere;line-height:1.45}.agent-activity-previous{display:grid;gap:4px;margin-top:7px;color:var(--faint);font-size:11px}.agent-activity-previous:empty{display:none}.agent-activity-previous div::before{content:"✓ ";color:var(--green)}.agent-activity-previous .activity-failed::before{content:"× ";color:#ff8f8f}.agent-activity-previous .activity-blocked::before{content:"⊘ ";color:#f2c66d}';document.head.append(studioAgentStyle);
const state={mode:'chat',tier:'fast',advancedAllowed:false,advanced:'logfare/kimi-k3',reasoning:'low',permission:'low',messages:[],chatId:null,abort:null,streaming:false};const fast='poolside/laguna-xs-2.1',smart='poolside/laguna-s-2.1';const reasoningModes=[['none','None'],['low','Light'],['medium','Medium'],['xhigh','Max']];const permissionSelect=document.createElement('select');permissionSelect.id='permissions';permissionSelect.className='mode permission-select';permissionSelect.setAttribute('aria-label','Agent permissions');permissionSelect.innerHTML='<option value="deny">Deny All</option><option value="low" selected>Allow low-risk</option><option value="ask">Always Ask</option><option value="full">Full Access</option>';document.querySelector('.right')?.insertBefore(permissionSelect,document.querySelector('#send'));function syncPermissionControl(){const active=state.mode==='agent'||state.mode==='swarm';permissionSelect.classList.toggle('visible',active);permissionSelect.value=state.permission}permissionSelect.onchange=()=>{state.permission=permissionSelect.value};
async function loadAccess(){try{const r=await fetch('/studio/api/models');const d=await r.json();state.advancedAllowed=Array.isArray(d.models)&&d.models.some(m=>m.access==='advanced');renderModelControls()}catch{}}const selectedModel=()=>state.tier==='advanced'?state.advanced:state.tier==='smart'?smart:fast;function requestControls(model){const controls={temperature:.5,chat_template_kwargs:{enable_thinking:state.reasoning!=='none'}};if(model.startsWith('logfare/'))controls.reasoning={effort:state.reasoning};return controls;}function renderModelControls(){const advanced=document.querySelector('#advancedModel');advanced.classList.toggle('hidden',state.tier!=='advanced');advanced.value=state.advanced;document.querySelector('#speed').textContent=state.tier==='advanced'?'Advanced':state.tier==='smart'?'Smart':'Fast';document.querySelector('#modelLabel').textContent=state.tier==='advanced'?'ADVANCED · '+advanced.options[advanced.selectedIndex].text:state.tier==='smart'?'SMART · Laguna S':'FAST · Laguna XS'};const storeKey='osaii.chats.v1';const feed=document.querySelector('#feed'),prompt=document.querySelector('#prompt'),send=document.querySelector('#send');let chatGeneration=0;
const escapeHtml=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function updateChatGPTAccountUi(){const button=document.querySelector('#chatgptLogin'),status=document.querySelector('#chatgptAuthStatus');if(!button)return;const connected=state.chatgptSignedIn===true;button.setAttribute('aria-label',connected?'Disconnect ChatGPT':'Sign in with ChatGPT');button.title=connected?'Disconnect ChatGPT':'Sign in with ChatGPT';if(status)status.textContent=connected?'Connected · '+chatGPTModels.length+' Codex models available, including image generation.':'Bring your own ChatGPT/Codex access. Your token stays in this browser and is only sent with requests you start.'}
function ensureChatGPTAccountCard(){const body=document.querySelector('#studioBody');if(!body||document.querySelector('#chatgptLogin'))return;const card=document.createElement('div');card.className='studio-card';card.innerHTML='<h3>ChatGPT / Codex</h3><p>Use your own ChatGPT account for its Codex model catalog and GPT Image generation. Your token stays in this browser and is only sent with requests you start.</p><div class="studio-row" style="margin-top:12px"><button class="chatgpt-signin-pill" id="chatgptLogin" type="button" aria-label="Sign in with ChatGPT" title="Sign in with ChatGPT" style="padding:0;border:0;background:transparent;border-radius:999px;overflow:hidden;display:inline-flex;cursor:pointer"><img src="https://raw.githubusercontent.com/EvanZhouDev/openai-oauth/refs/heads/main/assets/sign-in-with-chatgpt-button.png" alt="Sign in with ChatGPT" style="display:block;width:190px;height:auto"></button></div><p class="studio-muted" id="chatgptAuthStatus" style="margin-top:9px"></p>';body.append(card);document.querySelector('#chatgptLogin').onclick=()=>state.chatgptSignedIn?disconnectChatGPT():signInChatGPT();updateChatGPTAccountUi()}
Object.assign(state,{chatgptMode:false,chatgptSignedIn:false,chatgptModel:''});
const originalLoadChatGPTAccess=loadChatGPTAccess;
loadChatGPTAccess=async()=>{let session=null;try{const sdk=await chatGPTSdk();try{session=await sdk.getSession({refresh:true})}catch{session=await sdk.getSession({refresh:false}).catch(()=>null)}state.chatgptSignedIn=!!session;if(!session){chatGPTModels=[];chatGPTModelSelect.innerHTML='';state.chatgptMode=false;state.advanced='logfare/kimi-k3';renderModelControls();updateChatGPTAccountUi();return}state.chatgptModel=state.chatgptModel||'gpt-5.4-mini';try{const headers=await sdk.openaiAuthHeaders();const response=await fetch('/studio/api/chatgpt/models',{headers,cache:'no-store'});if(!response.ok)throw new Error('Codex model catalog returned '+response.status);const data=await response.json().catch(()=>({}));chatGPTModels=Array.isArray(data.data)?data.data:[];chatGPTModelSelect.innerHTML=chatGPTModels.length?chatGPTModels.map(model=>'<option value="chatgpt/'+escapeHtml(model.id)+'">'+escapeHtml(model.name||model.id)+'</option>').join(''):'<option value="chatgpt/gpt-5.4-mini">GPT-5.4 mini</option>'}catch(error){chatGPTModels=[];chatGPTModelSelect.innerHTML='<option value="chatgpt/gpt-5.4-mini">GPT-5.4 mini</option>';const status=document.querySelector('#chatgptAuthStatus');if(status)status.textContent='Connected to ChatGPT · model catalog unavailable ('+(error?.message||'retrying')+'). You can still try GPT-5.4 mini.'}state.chatgptSignedIn=true;renderModelControls();updateChatGPTAccountUi()}catch(error){state.chatgptSignedIn=false;updateChatGPTAccountUi();const status=document.querySelector('#chatgptAuthStatus');if(status)status.textContent=error?.message||'ChatGPT session could not be restored. Try Sign in again.'}};
const sessionCatalogLoadChatGPTAccess=loadChatGPTAccess;
loadChatGPTAccess=async()=>{await sessionCatalogLoadChatGPTAccess();if(state.chatgptSignedIn&&!chatGPTModels.length){chatGPTModels=[{id:'gpt-5.4-mini',name:'GPT-5.4 mini'}];chatGPTModelSelect.innerHTML='<option value="chatgpt/gpt-5.4-mini">GPT-5.4 mini</option>';state.chatgptModel=state.chatgptModel||'gpt-5.4-mini'}renderModelControls();updateChatGPTAccountUi();return state.chatgptSignedIn};
const sessionAwareUpdateChatGPTAccountUi=updateChatGPTAccountUi;
updateChatGPTAccountUi=()=>{sessionAwareUpdateChatGPTAccountUi();const button=document.querySelector('#chatgptLogin'),status=document.querySelector('#chatgptAuthStatus');if(!button)return;if(state.chatgptSignedIn){button.classList.add('connected');button.innerHTML='<span style="display:inline-flex;align-items:center;gap:8px;padding:10px 16px;background:#123c29;color:#e8fff2;border:1px solid rgba(52,209,126,.45);border-radius:999px;font:700 12px Manrope">✓ Connected to ChatGPT · Disconnect</span>';if(status)status.textContent='Connected · '+chatGPTModels.length+' Codex model'+(chatGPTModels.length===1?'':'s')+' available.'}else{button.classList.remove('connected');button.innerHTML='<img src="https://raw.githubusercontent.com/EvanZhouDev/openai-oauth/refs/heads/main/assets/sign-in-with-chatgpt-button.png" alt="Sign in with ChatGPT" style="display:block;width:190px;height:auto">'}};
const catalogReadyLoadChatGPTAccess=loadChatGPTAccess;
loadChatGPTAccess=async()=>{const callback=new URLSearchParams(location.search);if(callback.has('code')||callback.has('error')){try{const sdk=await chatGPTSdk();const session=await sdk.completeLogin();if(session)state.chatgptSignedIn=true}catch(error){const status=document.querySelector('#chatgptAuthStatus');if(status)status.textContent=error?.message||'ChatGPT callback could not be completed.'}}return catalogReadyLoadChatGPTAccess()};
signInChatGPT=async()=>{try{const sdk=await chatGPTSdk();const result=await sdk.startLogin({callbackPath:'/studio',returnTo:'/studio'});if(result?.status==='needs-extension'){const status=document.querySelector('#chatgptAuthStatus');if(status)status.innerHTML='<a href="'+escapeHtml(result.installUrl)+'" target="_blank" rel="noreferrer">'+chatGPTInstallHint+'</a>'}}catch(error){const status=document.querySelector('#chatgptAuthStatus');if(status)status.textContent=error?.message||'ChatGPT sign-in could not start.'}};
setTimeout(()=>loadChatGPTAccess(),150);setTimeout(()=>loadChatGPTAccess(),1_200);
const baseDisconnectChatGPT=disconnectChatGPT;
disconnectChatGPT=async()=>{try{await baseDisconnectChatGPT()}finally{state.chatgptMode=false;state.advanced='logfare/kimi-k3';renderModelControls()}};
const baseRenderModelControls=renderModelControls;
renderModelControls=()=>{const family=q('#speed'),advanced=q('#advancedModel'),chatgpt=q('#chatgptModel'),chatgptFamily=q('#chatgptFamily');const usingChatGPT=state.chatgptMode===true||(typeof state.advanced==='string'&&state.advanced.startsWith('chatgpt/'));if(family){family.value=usingChatGPT?'chatgpt':state.tier}if(advanced){advanced.classList.toggle('hidden',usingChatGPT||state.tier!=='advanced');advanced.disabled=usingChatGPT||state.tier!=='advanced';if(!usingChatGPT&&state.tier==='advanced'&&state.advanced)advanced.value=state.advanced}if(chatgpt){chatgpt.classList.toggle('hidden',!usingChatGPT);chatgpt.disabled=!usingChatGPT;if(usingChatGPT&&state.advanced)chatgpt.value=state.advanced}if(chatgptFamily){chatgptFamily.hidden=false;chatgptFamily.disabled=!state.chatgptSignedIn||!chatGPTModels.length;chatgptFamily.textContent=state.chatgptSignedIn&&chatGPTModels.length?'ChatGPT / Codex':'ChatGPT / Codex (sign in)'}document.querySelector('#modelLabel').textContent=usingChatGPT?'CHATGPT · '+(chatgpt?.options?.[chatgpt.selectedIndex]?.text||state.chatgptModel||'Codex'):state.tier==='advanced'?'ADVANCED · '+(advanced?.options?.[advanced.selectedIndex]?.text||'Advanced'):state.tier==='smart'?'SMART · Laguna S':'FAST · Laguna XS'};
const ALLOWED=new Set(['P','BR','STRONG','EM','B','I','CODE','PRE','BLOCKQUOTE','UL','OL','LI','H1','H2','H3','H4','A','HR','SPAN','DEL','TABLE','THEAD','TBODY','TR','TH','TD']);
function sanitize(h){const t=document.createElement('template');t.innerHTML=h;t.content.querySelectorAll('*').forEach(el=>{for(const a of [...el.attributes]){if(!(a.name==='href'&&/^https?:|^mailto:|^#/.test(a.value)))el.removeAttribute(a.name)}if(!ALLOWED.has(el.tagName)){el.replaceWith(...el.childNodes);return}if(el.tagName==='A'&&!el.getAttribute('href'))el.removeAttribute('href')});return t.innerHTML}
function renderMarkdown(src){if(!src)return '';if(typeof marked!=='undefined'&&marked.parse){try{return sanitize(marked.parse(src,{gfm:true,breaks:true}))}catch(e){}}return escapeHtml(src).replace(/\\n/g,'<br>')}
function postRender(el){el.querySelectorAll('pre').forEach(pre=>{if(pre.querySelector('.copy-btn'))return;const btn=document.createElement('button');btn.className='copy-btn';btn.textContent='Copy';btn.onclick=()=>{navigator.clipboard?.writeText(pre.querySelector('code')?.textContent||'').then(()=>{btn.textContent='Copied';setTimeout(()=>btn.textContent='Copy',1500)})};pre.prepend(btn)});el.querySelectorAll('a').forEach(a=>{a.target='_blank';a.rel='noopener noreferrer'})}
function renderMessage(role,text,el){if(role==='assistant'){el.innerHTML=renderMarkdown(text);postRender(el)}else el.textContent=text}
let rafTimer=null,streamEpoch=0,stateFollowing=true;const streamNearBottom=()=>feed.scrollHeight-feed.scrollTop-feed.clientHeight<120;function updateFollowArrow(){followStream.hidden=!state.streaming||stateFollowing}function followLatest(){stateFollowing=true;feed.scrollTop=feed.scrollHeight;updateFollowArrow()}feed.addEventListener('scroll',()=>{stateFollowing=streamNearBottom();updateFollowArrow()});followStream.onclick=followLatest;function clearStreamingFrame(){streamEpoch++;if(rafTimer){cancelAnimationFrame(rafTimer);rafTimer=null}}function renderStreaming(el,content){state.streaming=true;const epoch=++streamEpoch;if(rafTimer)cancelAnimationFrame(rafTimer);rafTimer=requestAnimationFrame(()=>{rafTimer=null;if(epoch!==streamEpoch||!el.isConnected)return;el.innerHTML=escapeHtml(content).replace(/\\n/g,'<br>');el.classList.add('streaming');if(stateFollowing)feed.scrollTop=feed.scrollHeight;updateFollowArrow()})}
function setTyping(el,on){if(on){if(!el.querySelector('.dots')){const d=document.createElement('span');d.className='dots';d.innerHTML='<i></i><i></i><i></i>';el.append(d)}el.classList.add('typing')}else{el.querySelector('.dots')?.remove();el.classList.remove('typing')}}
function createAgentActivity(label){const panel=document.createElement('div');panel.className='agent-activity';panel.hidden=true;panel.innerHTML='<div class="agent-activity-head"><span>'+escapeHtml(label||'Agent activity')+'</span><span class="agent-activity-label">Live</span></div><div class="agent-activity-current">Waiting for a tool call…</div><div class="agent-activity-previous"></div>';feed.append(panel);const current=panel.querySelector('.agent-activity-current'),previous=panel.querySelector('.agent-activity-previous');const formatArgs=value=>{const text=String(value||'').replace(/\s+/g,' ').trim();return text.length>280?text.slice(0,280)+'…':text};return{call(name,detail){panel.hidden=false;current.textContent='Running '+name+(detail?' · '+formatArgs(detail):'')+'…';current.dataset.tool=name},args(name,partial){panel.hidden=false;const text=formatArgs(partial);current.textContent='Running '+name+(text?' · args: '+text:' · waiting for args…')+'…';current.dataset.tool=name;current.dataset.args=String(partial||'')},complete(name,status='ok'){panel.hidden=false;const row=document.createElement('div');const kind=status===true||status==='ok'?'ok':status===false?'failed':status;row.className='activity-'+kind;row.textContent=kind==='blocked'?'Blocked by permissions: '+name:kind==='failed'?'Failed: '+name:'Completed: '+name;previous.append(row);current.textContent='No active tool call';delete current.dataset.tool;delete current.dataset.args},phase(text){panel.hidden=false;current.textContent=text},finish(){if(!previous.children.length)panel.hidden=true;else current.textContent='All calls complete'}}}
let liveActivity=null;
function addMessage(role,text,opts={}){const el=document.createElement('article');el.className='message '+role;renderMessage(role,text,el);if(opts.typing)setTyping(el,true);if(opts.typing&&(state.mode==='agent'||state.mode==='swarm'))liveActivity=createAgentActivity(state.mode==='swarm'?'Swarm activity':'Agent activity');feed.append(el);if(role==='user')stateFollowing=true;if(stateFollowing)feed.scrollTop=feed.scrollHeight;return el}
const backupStoreKey='osaii.chats.backup.v1';
function chatStorages(){const out=[];for(const getter of [()=>localStorage,()=>sessionStorage]){try{out.push(getter())}catch{}}return out}
function titleText(value){if(typeof value==='string')return value;if(Array.isArray(value))return value.map(part=>typeof part==='string'?part:(part?.text||part?.content||'')).join(' ');if(value&&typeof value==='object')return value.text||value.content||'';return String(value??'')}
function chatTitleFromMessages(messages){const first=(Array.isArray(messages)?messages:[]).find(m=>m&&m.role==='user');return autoChatTitle(titleText(first?.content))}
function normalizeChat(chat){if(!chat||typeof chat.id!=='string'||!Array.isArray(chat.messages))return null;const title=typeof chat.title==='string'&&chat.title.trim()&&chat.title.trim()!=='New conversation'?chat.title.trim():chatTitleFromMessages(chat.messages);return {...chat,title:title||'New conversation'} }
function loadChats(){for(const storage of chatStorages()){for(const key of [storeKey,backupStoreKey]){try{const parsed=JSON.parse(storage.getItem(key)||'null');if(Array.isArray(parsed))return parsed.map(normalizeChat).filter(Boolean)}catch{}}}return[]}
function saveChats(c){const value=JSON.stringify(c);for(const storage of chatStorages()){try{storage.setItem(backupStoreKey,value);storage.setItem(storeKey,value);return}catch{}}window.__osaiiUnsavedChats=c}
function renderHistory(){const list=document.querySelector('#histList');const chats=loadChats();if(chats.length)saveChats(chats);list.innerHTML='';if(!chats.length){list.innerHTML='<div class="hist-empty">No conversations yet</div>';return}for(const c of chats){const b=document.createElement('button');b.className='hist-item'+(c.id===state.chatId?' active':'');const t=document.createElement('span');t.className='hist-title';t.textContent=c.title||'New conversation';const d=document.createElement('span');d.className='hist-del';d.textContent='×';d.onclick=e=>{e.stopPropagation();deleteChat(c.id)};b.append(t,d);b.onclick=()=>openChat(c.id);list.append(b)}}
function newChat(){state.abort?.abort();state.abort=null;state.chatId=null;state.messages=[];state.streaming=false;feed.innerHTML='';document.querySelector('#welcome').classList.remove('hidden');send.disabled=false;send.textContent='Send ↗';renderHistory()}
function openChat(id){state.abort?.abort();state.abort=null;const c=loadChats().find(x=>x.id===id);if(!c)return;state.chatId=c.id;state.messages=c.messages.slice();state.mode=c.mode;state.tier=c.tier||(c.smart?'smart':'fast');state.advanced=c.advanced||state.advanced;state.reasoning=c.reasoning;state.permission=c.permission||'low';document.querySelectorAll('[data-mode]').forEach(x=>x.classList.toggle('active',x.dataset.mode===c.mode));document.querySelector('#swarmNote').classList.toggle('hidden',c.mode!=='swarm');syncPermissionControl();renderModelControls();document.querySelector('#reasoning').textContent='Reasoning: '+(reasoningModes.find(m=>m[0]===c.reasoning)?.[1]||'Light');feed.innerHTML='';c.messages.forEach(m=>addMessage(m.role,m.content));document.querySelector('#welcome').classList.add('hidden');setStreaming(false);prompt.focus();renderHistory()}
function deleteChat(id){const chats=loadChats().filter(c=>c.id!==id);saveChats(chats);if(id===state.chatId){state.chatId=null;state.messages=[];feed.innerHTML='';document.querySelector('#welcome').classList.remove('hidden');setStreaming(false)}renderHistory()}
function autoChatTitle(text){let title=String(text||'').replace(/\\s+/g,' ').replace(/^[\\s.!?]+|[\\s.!?]+$/g,'').split(/[.!?](?:\\s|$)/)[0].trim();title=title.replace(/^(please|can you|could you|would you)\\s+/i,'').replace(/^make (the )?source code for /i,'').replace(/^help me (to )?/i,'');if(title.length>54)title=title.slice(0,54).replace(/\\s+\\S*$/,'');return title?title.charAt(0).toUpperCase()+title.slice(1):'New conversation'}
function saveCurrentChat(){if(!state.messages.length||!state.messages.some(m=>m&&m.role==='user'))return;if(!state.chatId)state.chatId=Date.now().toString(36)+Math.random().toString(36).slice(2,6);const title=chatTitleFromMessages(state.messages);const chats=loadChats();const idx=chats.findIndex(c=>c.id===state.chatId);const chat={id:state.chatId,title:title||'New conversation',mode:state.mode,tier:state.tier,advanced:state.advanced,reasoning:state.reasoning,permission:state.permission,messages:state.messages.slice(),updatedAt:Date.now()};if(idx>=0)chats[idx]=chat;else chats.unshift(chat);chats.sort((a,b)=>b.updatedAt-a.updatedAt);saveChats(chats.slice(0,50));renderHistory()}
function setStreaming(on){state.streaming=on;if(on)stateFollowing=true;send.textContent=on?'Stop':'Send ↗';send.disabled=false;updateFollowArrow()}
function stopStream(){state.abort?.abort()}
document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;document.querySelectorAll('[data-mode]').forEach(x=>x.classList.toggle('active',x===b));document.querySelector('#swarmNote').classList.toggle('hidden',state.mode!=='swarm');syncPermissionControl()});
document.querySelector('#speed').onclick=()=>{};document.querySelector('#advancedModel').onchange=e=>{state.advanced=e.target.value;state.chatgptMode=false;renderModelControls()};
document.querySelector('#reasoning').onclick=()=>{};
document.querySelector('#speed').onchange=e=>{const family=e.target.value;state.chatgptMode=family==='chatgpt';if(family==='chatgpt'){state.tier='advanced';state.chatgptModel=state.chatgptModel||chatGPTModels[0]?.id||'gpt-5.4-mini';state.advanced='chatgpt/'+state.chatgptModel}else if(family==='advanced'){state.tier='advanced';state.advanced=state.advanced?.startsWith('logfare/')?state.advanced:'logfare/kimi-k3'}else if(family==='smart'){state.tier='smart';state.advanced='logfare/kimi-k3'}else{state.tier='fast';state.advanced='logfare/kimi-k3'}renderModelControls()};document.querySelector('#chatgptModel').onchange=e=>{state.chatgptMode=true;state.chatgptModel=e.target.value.slice(8);state.tier='advanced';state.advanced=e.target.value;renderModelControls()};document.querySelector('#reasoning').onchange=e=>{state.reasoning=e.target.value};
const originalOpenChat=openChat;openChat=id=>{originalOpenChat(id);const reasoning=q('#reasoning');if(reasoning&&!reasoning.options.length)reasoning.innerHTML='<option value="none">Reasoning: None</option><option value="low">Reasoning: Light</option><option value="medium">Reasoning: Medium</option><option value="xhigh">Reasoning: Max</option>';if(reasoning)reasoning.value=state.reasoning||'low';renderModelControls()};
const modelFamilyRender=renderModelControls;renderModelControls=()=>{modelFamilyRender();const option=q('#chatgptFamily');if(option){option.hidden=false;option.disabled=!state.chatgptSignedIn||!chatGPTModels.length;option.textContent=state.chatgptSignedIn&&chatGPTModels.length?'ChatGPT / Codex':'ChatGPT / Codex (sign in)'}};
agentEffort=()=>q('#reasoning')?.value||'low';
setInterval(()=>{const option=q('#chatgptFamily');if(option){option.hidden=false;option.disabled=!state.chatgptSignedIn||!chatGPTModels.length;option.textContent=state.chatgptSignedIn&&chatGPTModels.length?'ChatGPT / Codex':'ChatGPT / Codex (sign in)'}},250);
const settleAgentActivity=()=>document.querySelectorAll('.agent-activity').forEach(panel=>{if(!panel.querySelector('.activity-failed,.activity-blocked'))return;const current=panel.querySelector('.agent-activity-current');if(current?.textContent==='All calls complete')current.textContent='Completed with failures';const label=panel.querySelector('.agent-activity-label');if(label&&label.textContent==='Live')label.textContent='Attention'});new MutationObserver(settleAgentActivity).observe(document.body,{subtree:true,childList:true});
renderModelControls=()=>{const family=q('#speed'),advanced=q('#advancedModel'),chatgpt=q('#chatgptModel'),chatgptFamily=q('#chatgptFamily');const usingChatGPT=state.chatgptMode===true||(typeof state.advanced==='string'&&state.advanced.startsWith('chatgpt/'));if(family)family.value=usingChatGPT?'chatgpt':state.tier;if(advanced){advanced.classList.toggle('hidden',usingChatGPT||state.tier!=='advanced');advanced.disabled=usingChatGPT||state.tier!=='advanced';if(!usingChatGPT&&state.tier==='advanced'&&state.advanced)advanced.value=state.advanced}if(chatgpt){chatgpt.classList.toggle('hidden',!usingChatGPT);chatgpt.disabled=!usingChatGPT;if(usingChatGPT&&state.advanced)chatgpt.value=state.advanced}if(chatgptFamily){chatgptFamily.hidden=false;chatgptFamily.disabled=!state.chatgptSignedIn||!chatGPTModels.length;chatgptFamily.textContent=state.chatgptSignedIn&&chatGPTModels.length?'ChatGPT / Codex':'ChatGPT / Codex (sign in)'}if(family)document.querySelector('#modelLabel').textContent=usingChatGPT?'CHATGPT · '+(chatgpt?.options?.[chatgpt.selectedIndex]?.text||state.chatgptModel||'Codex'):state.tier==='advanced'?'ADVANCED · '+(advanced?.options?.[advanced.selectedIndex]?.text||'Advanced'):state.tier==='smart'?'SMART · Laguna S':'FAST · Laguna XS'};
document.querySelectorAll('.chip').forEach(c=>c.onclick=()=>{prompt.value=c.dataset.prompt;run()});
function readableError(data,fallback='The request could not be completed.'){const value=data?.error??data?.message;if(typeof value==='string'&&value.trim())return value;if(value&&typeof value==='object'&&typeof value.message==='string')return value.message;try{if(value)return JSON.stringify(value)}catch{}return fallback}
async function ask(messages,model){const r=await fetch('/studio/api/chat',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({model,messages,...requestControls(model)})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(readableError(d));return d.choices?.[0]?.message?.content||'No response returned.'}
function reasoningBudgetNotice(){return 'The model used its full reasoning budget before producing a final answer. Try again with a larger token budget or a shorter prompt.'}
async function askStream(messages,model,onDelta,onReasoning){const controller=new AbortController();state.abort=controller;const requestBody={model,messages,...requestControls(model),stream:true,stream_options:{include_usage:true}};const r=await fetch('/studio/api/chat',{method:'POST',headers:{'content-type':'application/json',accept:'text/event-stream'},body:JSON.stringify(requestBody),signal:controller.signal});if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(readableError(d))}if(!r.body)return '';const reader=r.body.getReader();const dec=new TextDecoder();let buf='',raw='',content='',reasoning='',sawSse=false;const consumeLine=line=>{const text=line.trim();if(!text.startsWith('data:'))return;sawSse=true;const payload=text.slice(5).trim();if(payload==='[DONE]')return;let j;try{j=JSON.parse(payload)}catch{return}if(j.error)throw new Error(readableError(j,'Upstream provider error'));const delta=j.choices?.[0]?.delta||{};const thought=typeof delta.reasoning_content==='string'?delta.reasoning_content:'';if(thought){reasoning+=thought;if(onReasoning)onReasoning(reasoning)}const piece=typeof delta.content==='string'?delta.content:'';if(piece){content+=piece;onDelta(content)}};const flushComplete=()=>{let i;while((i=buf.indexOf('\\n'))>=0){consumeLine(buf.slice(0,i));buf=buf.slice(i+1)}};try{while(true){const {done,value}=await reader.read();if(done)break;const text=dec.decode(value,{stream:true});raw+=text;buf+=text;flushComplete()}const tail=dec.decode();if(tail){raw+=tail;buf+=tail}flushComplete();if(buf.trim())consumeLine(buf)}catch(e){if(e.name!=='AbortError')throw e}if(!sawSse&&raw.trim()){try{const j=JSON.parse(raw);const fallback=j.choices?.[0]?.message?.content;if(typeof fallback==='string'){content=fallback;onDelta(content)}else if(j.choices?.[0]?.message?.reasoning_content){content=reasoningBudgetNotice();onDelta(content)}}catch{}}return content|| (reasoning ? reasoningBudgetNotice() : '')}
async function run(){const text=prompt.value.trim();if(!text)return;prompt.value='';document.querySelector('#welcome').classList.add('hidden');addMessage('user',text);state.messages.push({role:'user',content:text});saveCurrentChat();let el=null;try{let out;if(state.mode==='swarm'){el=addMessage('assistant','Launching 300 independent passes…');setTyping(el,true);send.disabled=true;send.textContent='Working…';const tasks=Array.from({length:300},(_,i)=>ask([{role:'system',content:'You are researcher '+(i+1)+' in a parallel team. Give one compact, concrete angle. Do not repeat the task.'},{role:'user',content:text}],fast).catch(()=>''));const findings=(await Promise.all(tasks)).filter(Boolean).slice(0,300);setTyping(el,false);el.textContent='Delegator is combining '+findings.length+' perspectives…';out=await askStream([{role:'system',content:'You are the lead delegator. Synthesize the team findings into a direct, useful answer with a recommendation.'},{role:'user',content:'Task: '+text+'\\n\\nFindings:\\n'+findings.join('\\n---\\n')}],smart,c=>{setTyping(el,false);renderStreaming(el,c)});}else{setStreaming(true);const system=state.mode==='agent'?'You are a practical web-aware task agent. Break work into clear steps, state what you can verify, and give the user a direct result.':'You are a clear, helpful assistant for everyday people.';el=addMessage('assistant','',{typing:true});out=await askStream([{role:'system',content:system},...state.messages],selectedModel(),c=>{setTyping(el,false);renderStreaming(el,c)});}if(out){setTyping(el,false);el.classList.remove('streaming');renderMessage('assistant',out,el);state.messages.push({role:'assistant',content:out});}else if(state.abort?.signal.aborted){el.remove();}else{setTyping(el,false);el.classList.remove('streaming');renderMessage('assistant','No response returned.',el);state.messages.push({role:'assistant',content:'No response returned.'});}saveCurrentChat()}catch(e){if(e.name!=='AbortError'){if(el){setTyping(el,false);el.classList.remove('streaming');renderMessage('assistant','Could not complete that: '+e.message,el)}else{addMessage('assistant','Could not complete that: '+e.message)}}}finally{clearStreamingFrame();state.abort=null;setStreaming(false);send.disabled=false;prompt.focus()}}
const baseRun=run;run=async()=>{const activeMode=qa('[data-mode].active')[0]?.dataset.mode;if(activeMode)state.mode=activeMode;if(state.mode==='swarm')return swarmRun();return baseRun()};
send.onclick=()=>{if(state.streaming){stopStream()}else{run()}};prompt.onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();if(!state.streaming)run()}};renderModelControls();loadAccess();renderHistory();window.__osaiiState=state;` }
